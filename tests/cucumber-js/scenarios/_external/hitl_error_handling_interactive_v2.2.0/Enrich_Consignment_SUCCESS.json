{
  "workflowDefinitionURI": "${ctx:workflowDefinitionUri}",
  "workflowInstanceId": "${workflowInstanceId}",
  "correlationId": "${correlationId}",
  "command": {
    "type": "ASYNC_RESP",
    "id": "${uuid}",
    "source": "MockCapabilityService",
    "timestamp": "${now}",
    "status": "SUCCESS",
    "payload": {
      "consignmentImportEnrichedId": "ENR-${uuid}",
      "consignmentImportEnrichedURI": "s3://${env:START_S3_BUCKET}/enriched/${correlationId}.json",
      "consignmentImportExtId": "EXT-${uuid}",
      "consignmentImportExtURI": "s3://${env:START_S3_BUCKET}/ext/${correlationId}.json"
    }
  }
}
