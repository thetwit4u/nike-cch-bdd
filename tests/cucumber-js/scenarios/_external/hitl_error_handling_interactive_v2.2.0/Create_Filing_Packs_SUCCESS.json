{
  "workflowDefinitionURI": "${ctx:workflowDefinitionUri}",
  "workflowInstanceId": "${workflowInstanceId}",
  "correlationId": "${correlationId}",
  "command": {
    "type": "ASYNC_RESP",
    "id": "${uuid}",
    "source": "MockCapabilityService",
    "timestamp": "${now}",
    "status": "SUCCESS",
    "payload": {
      "importFilingPacks": [
        { "filingPackId": "packA", "filingPackURI": "s3://example/packs/${correlationId}/packA.json" },
        { "filingPackId": "packB", "filingPackURI": "s3://example/packs/${correlationId}/packB.json" }
      ]
    }
  }
}
